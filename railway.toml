[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt"

[build.nixpacks]
nixPkgs = ["ghostscript", "python39"]

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3